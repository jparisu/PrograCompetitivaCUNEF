// C++ fast I/O — put these two lines first, inside main():
ios::sync_with_stdio(false);
cin.tie(nullptr);
// Then read/print as usual:  cin >> x;   cout << x << '\n';
// (use '\n', never endl inside loops — endl flushes the buffer every time).
